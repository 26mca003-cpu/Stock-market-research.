# Presentation

Add the final competition slide deck to this directory. The repository is structured so the deck can be referenced from the root README and `submission.yaml` without changing the submission metadata format.

